import 'package:flutter/material.dart';

class CreateClassPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Buat Kelas Baru')),
      body: Center(child: Text('Halaman Buat Kelas')),
    );
  }
}
